---
title: Experiment 07
sidebar_label: EXP 07
---

### **Aim**
> To study and execute commands for scheduling automated tasks and managing user/group accounts.


### **Theory**
- **`cron` / `crontab`**
  - A time-based job scheduler used to run scripts or commands at specified intervals, dates, or times.
  - *Syntax:* `crontab -e` (to edit the current user's cron jobs).

- **`at`**
  - Used to schedule a command or script to be executed exactly once at a specific time in the future.
  - *Syntax:* `at [time]` (e.g., at 10:30 PM).

- **`useradd`**
  - Creates a new user account on the Linux system.
  - *Syntax:* `sudo useradd [options] [username]` (e.g., -m to create a home directory).

- **`usermod`**
  - Modifies a user account's properties, such as adding them to a new group.
  - *Syntax:* `sudo usermod -aG [groupname] [username]`

- **`passwd`**
  - Used to set or update a user's authentication token (password).
  - *Syntax:* `sudo passwd [username]`

- **`groupadd`**
  - Creates a new user group on the system.
  - *Syntax:* `sudo groupadd [groupname]`


### **Commands**
```bash

$ sudo groupadd developers

$ sudo useradd -m testuser1

$ sudo passwd testuser1
New password: 
Retype new password: 
passwd: password updated successfully

$ sudo usermod -aG developers testuser1

$ cat /etc/group | grep developers
developers:x:1002:testuser1

$ at 05:00 PM
warning: commands will be executed using /bin/sh
at> echo "System update reminder" > /home/student/reminder.txt
at> <EOT> (Press Ctrl+D to save)
job 1 at Wed Mar 19 17:00:00 2026

$ crontab -e
(Inside the editor, add the following line to run a backup script every day at 2 AM)
0 2 * * * /home/student/backup.sh
(Save and exit)
crontab: installing new crontab

```

### **Conclusion**
> User and group administration tasks, including creating accounts, setting passwords, and assigning group memberships (`useradd`, `groupadd`, `usermod`, `passwd`), were successfully executed. Additionally, both one-time (at) and recurring (cron) task scheduling operations were successfully configured